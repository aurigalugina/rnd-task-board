# CLAUDE.md

File ini jadi panduan untuk Claude Code (claude.ai/code) saat kerja di repo ini.

## Living Memory — WAJIB DIBACA

File ini bukan dokumen statis sekali-tulis, tapi **living memory** — harus terus diperbarui seiring project berjalan. Setiap sesi Claude Code berikutnya:

- **Update file ini** kalau menemukan keputusan arsitektur baru, aturan bisnis baru, gotcha/jebakan yang baru ditemukan, atau asumsi lama di sini yang ternyata sudah berubah/salah.
- **Jangan biarkan basi.** Kalau kode sudah menyimpang dari yang tertulis di sini (misalnya modul baru sudah diimplementasi, atau pattern berubah), perbarui bagian terkait saat itu juga — jangan tunggu diminta.
- **Tulis ringkas, bukan naratif.** Tambahkan fakta + alasan singkat (kenapa keputusan itu diambil), bukan cerita panjang proses debugging.
- Kalau ragu apakah sesuatu layak masuk sini: masuk sini kalau itu hal yang **tidak bisa ditebak cuma dari baca kode** (keputusan arsitektur, alasan di balik pola tertentu, batasan yang harus dipatuhi) — bukan hal yang sudah jelas dari struktur file.

## Decision Log — wajib dicatat tiap final decision

Setiap kali sebuah keputusan sudah **final** (bukan lagi didiskusikan/dipertimbangkan — sudah disepakati untuk dijalankan), catat dulu ke decision log **sebelum** lanjut implementasi:

- Lokasi: `docs/decision-log/decision-log-[context]-YYYYMMDD.md`
- `[context]` = slug singkat kebab-case yang menjelaskan topik keputusan, mis. `kanban-state-machine`, `role-based-auth`, `sqlc-migration`.
- `YYYYMMDD` = tanggal keputusan difinalisasi.
- Satu file per keputusan/topik diskusi (bukan satu file besar yang terus ditambah) — supaya history tiap keputusan tetap terlacak per tanggal.
- Isi minimal: **konteks/masalah**, **keputusan yang diambil**, **alasan** (termasuk alternatif yang ditolak kalau relevan), dan **dampak/file yang terpengaruh**.
- Setelah decision log dibuat, kalau keputusan itu juga mengubah pola/arsitektur yang dipakai jangka panjang, sinkronkan ringkasannya ke bagian relevan di CLAUDE.md ini juga (lihat `Living Memory` di atas) — decision log mencatat kronologi "kenapa & kapan", CLAUDE.md mencatat state final "begini caranya sekarang".

## Testing — wajib sebelum task code dianggap selesai

Setiap task yang menyentuh logika kode (fitur baru, bug fix, refactor — bukan cuma dokumentasi/config) **wajib ada unit test-nya sebelum task itu dilaporkan selesai**, bukan ditunda ke sesi/fase testing terpisah nanti.

- **Backend (Go):** tambah/perbarui `_test.go` untuk logic yang disentuh — prioritaskan logika computed murni (`computeVerdict`, kalkulasi `actual_pct`/`expected_pct`, rollup mingguan, dsb) karena itu yang paling gampang salah diam-diam dan paling sering disebut sebagai prinsip tidak bisa ditawar di BRD. Jalankan `go test ./...` sampai hijau sebelum lapor selesai.
- **Frontend:** minimal `npm run check` tiap ubah `.svelte`/`.ts`. Kalau ada logic non-trivial yang ditambahkan (bukan cuma markup/styling — mis. logic di store, kalkulasi, parsing), tulis unit test-nya juga. Test runner: **vitest** (`npm run test`), sudah terpasang sejak Fase 1 — logic murni yang layak ditest biasanya diekstrak ke file `.ts` biasa di `lib/` (bukan ditinggal inline di `<script>` komponen), supaya bisa di-import & ditest tanpa render komponen. Contoh: `lib/dateRange.ts`, `lib/comments.ts`, `lib/dashboardStats.ts`.
- Fase 8 roadmap (lihat `docs/decision-log/decision-log-development-roadmap-20260808.md`) **sudah selesai** — audit menyeluruh logic computed murni yang lolos tanpa test dari fase-fase sebelumnya, ditambahkan sekaligus: `auth.RequireRole` (matching role, security-critical), `dailytask.isWeekend`/`parseDateRange`, `weeklyplan.parseWeekStart`, dan `dashboardStats.computeDashboardStats`/`truncateName` (diekstrak dari `routes/+page.svelte` khusus supaya testable). Kalau nemu logic computed murni baru yang belum ada test-nya, jangan tunggu "fase testing" lagi — langsung tambah saat itu juga (lihat aturan di atas).
- Task dengan perubahan yang murni tidak testable via unit test (mis. wiring UI yang divalidasi manual di browser, config) boleh cukup divalidasi manual — tapi kalau ada logic yang bisa diuji di dalamnya (mis. logic di dalam komponen/store), logic itu tetap wajib ditest.

## Project

R&D Ops — portal internal untuk tracking board tim, "Big Task" (level proyek), dan "Daily Task" (eksekusi harian) dengan mekanisme sign-off/verdict. Spek produk lengkap ada di `docs/`: `01-vision-product.md`, `02-brd.md`, `03-srs.md`, `04-architecture.md`, `05-api-contract.md`, `06-db-design.md`. Dokumen-dokumen ini adalah sumber kebenaran untuk aturan bisnis (BRD RULE-xx, SRS FR-xx) — baca bagian relevan sebelum implementasi endpoint baru atau mengubah logika computed-field.

**`myagenda-service/` adalah sub-project TERPISAH** (Go module, docker-compose, Makefile, README sendiri) — bukan bagian dari `backend`/`frontend` di atas. Fungsinya integrasi Push-to-HR (Fase 2), sengaja dipisah karena production-nya akan dideploy di server aplikasi HR sendiri, bukan bareng rnd-task-board. Lihat bagian "Integrasi HR — `myagenda-service`" di bawah dan `myagenda-service/README.md`.

**`claude-chat-service/` adalah sub-project TERPISAH lainnya** (Node.js/TypeScript, docker-compose sendiri — **sudah diimplementasi & smoke-tested 2026-08-11**) — bridge antara Claude Code dan client aplikasi, fondasi untuk mekanisme evolusi produk di `01-vision-product.md` §6 (change request via percakapan, dibantu agentic coding assistant), scope-nya dibuat general-purpose. Lihat bagian "Claude Chat Bridge — `claude-chat-service`" di bawah.

## Commands

```bash
make up             # docker compose up --build (postgres + backend + nginx, full stack)
make down
make migrate-up     # jalankan migration via image docker golang-migrate
make migrate-down   # rollback satu migration
make seed           # seed dev user (spv@rndops.local / password123) — lihat backend/db/seed/0001_seed_dev_user.sql untuk query manual link role setelahnya

# Versi dev (hot-reload) -- docker-compose.dev.yml, port sama (5432/8080) jadi
# jangan bareng-bareng sama `make up`. Frontend HMR di :5173 (bukan :8080, gak ada nginx).
make up-dev         # air (backend) + vite dev server asli (frontend), source di-bind-mount dari host
make down-dev
make seed-dev       # sama seperti `make seed` tapi target ke stack -f docker-compose.dev.yml
```

Backend (Go), dari `backend/`:
```bash
go mod tidy
go build ./...
go vet ./...
go run ./cmd/api        # butuh DATABASE_URL, fallback ke kredensial dev localhost:5432 kalau tidak diset
go test ./...           # wajib hijau sebelum task code dianggap selesai — lihat bagian Testing
```

Frontend (SvelteKit), dari `frontend/`:
```bash
npm install
npm run dev             # vite dev server, expect backend bisa diakses di /api/v1 (via proxy)
npm run build
npm run check           # svelte-check type checking — jalankan ini setelah ubah file .svelte/.ts
npm run test            # vitest — unit test untuk logic non-trivial (lihat bagian Testing)
```

Aplikasi tersaji di `http://localhost:8080` kalau dijalankan via `make up` (nginx proxy `/api/*` ke backend Go, dan menyajikan hasil build statis SvelteKit untuk selebihnya).

## Dev Environment (Docker)

`docker-compose.dev.yml` (2026-08-10) — versi hot-reload dari `docker-compose.yml` (yang itu murni buat build/prod: multi-stage Dockerfile, output statis, nginx). Detail lengkap & alasan: `docs/decision-log/decision-log-docker-dev-compose-20260810.md`.

- Backend: `backend/Dockerfile.dev` pakai [`air`](https://github.com/air-verse/air) (watch `.go`, auto rebuild+restart), source di-bind-mount (`./backend:/app`), Go module cache & `tmp` build dir pakai NAMED VOLUME (bukan host dir langsung) — `tmp` tetap dibuat sebagai mountpoint kosong di host tiap kali stack ini nyala (harmless, sudah di `.gitignore`).
- Frontend: image `node:20-alpine` polos (gak perlu Dockerfile sendiri), `npm install && npm run dev -- --host --port 5173` tiap start, `node_modules` pakai named volume (biar gak ke-overwrite bind mount host & gak install ulang tiap restart).
- **Base image backend sudah `golang:1.25-alpine`** (both `Dockerfile` dan `Dockerfile.dev`) sejak `github.com/xuri/excelize/v2` ditambahkan untuk fitur Import/Export — salah satu transitive dep-nya requires Go 1.25. `go.mod` directive ikut `go 1.25.0`. Jangan downgrade kembali ke 1.22 — `excelize` tidak bisa dikompilasi.
- **Gotcha historis (sudah tidak relevan):** `air@latest` dulu butuh Go lebih baru dari base `golang:1.22-alpine`; solusinya adalah `ENV GOTOOLCHAIN=auto` di `Dockerfile.dev`. Sekarang base image sudah 1.25, `GOTOOLCHAIN=auto` masih ada tapi tidak berpengaruh lagi — tidak perlu dihapus.
- **`vite.config.ts` proxy `/api` di-parameterize via `process.env.VITE_API_PROXY_TARGET`** (default tetap `http://localhost:8080` buat dev lokal biasa tanpa Docker — TIDAK ADA perubahan behavior kalau env var-nya gak di-set). Di `docker-compose.dev.yml`, frontend container set `VITE_API_PROXY_TARGET=http://backend:8080` (nama service di docker network) — kalau tetap hardcode `localhost:8080`, proxy dari dalam container frontend akan nyasar ke dirinya sendiri, bukan ke container backend.
- Port sama dengan versi build (`5432`/`8080`) — TIDAK bisa jalan bersamaan dengan `make up`, harus `make down` dulu.

## Arsitektur

Monolith modular, bukan microservices — satu binari Go, dipisah per domain secara logis via internal package, satu instance Postgres. Ini keputusan skala yang disengaja (lihat `04-architecture.md` §1); jangan usulkan pemisahan jadi banyak service.

**Alur request:** Nginx → build statis SvelteKit (adapter-static, tanpa SSR) untuk `/`, reverse-proxy ke backend Go/chi untuk `/api/*`.

**Pola modul backend** (`backend/internal/<domain>/handler.go`): tiap domain punya struct `Handler` yang membungkus `*pgxpool.Pool`, dibuat via `NewHandler(pool)`, method-nya langsung dipasang ke route chi di `cmd/api/main.go`. Tidak ada layer service/repository — handler query `pgx` langsung. Semua 10 modul yang didokumentasikan di `05-api-contract.md` (`board`, `bigtask`, `dailytask`, `user`, `comment`, `cheatsheet`, `upload`, `weeklyplan`, `reviewqueue`) sudah diimplementasi penuh (roadmap Fase 0–7 selesai) — kalau mau menambah modul baru di luar kontrak yang ada, ikuti pola yang sama (struktur file sama, gaya inline-SQL sama, bentuk struct request/response sama), dan pasang route baru ke grup `protected` di `main.go`.

**sqlc belum aktif.** Meskipun disebut di `04-architecture.md` sebagai pendekatan yang dituju, handler saat ini pakai query `pgx` mentah supaya scaffold ini bisa langsung dibangun tanpa langkah `sqlc generate`. `06-db-design.md` §5 mendokumentasikan pola query sqlc yang dituju sebagai referensi untuk migrasi nanti — jangan aktifkan tooling sqlc tanpa didiskusikan dulu, dan jangan asumsikan file `.sql` di `backend/db/queries/` sudah terpakai (folder itu masih kosong).

**Field yang dihitung saat baca (read-time) — jangan pernah disimpan sebagai kolom:** `actual_pct`, `expected_pct`, `verdict` (big task), `completion_rate`/`project_status` (board summary), `is_weekend` (day entry). Nilai-nilai ini bergantung pada waktu berjalan (`TODAY`) atau agregasi baris anak, dan wajib dihitung ulang tiap kali dibaca (lihat `04-architecture.md` §5.1, `06-db-design.md` §1). Ini keputusan sadar untuk menghindari kebutuhan cron job harian. Kalau menambah field turunan baru, ikuti pola ini, jangan disimpan permanen.

**Logika verdict** (`backend/internal/bigtask/handler.go: computeVerdict`): status `on_progress` bersifat netral berapa pun gap antara actual vs expected, sampai ada titik keputusan (sign-off, atau deadline lewat tanpa sign-off) — win/lose baru ditentukan di titik itu, bukan berdasarkan tertinggal jadwal di tengah jalan (BRD RULE-04/05/06).

**Sign-off** (`big_task_signoffs`): keberadaan baris = signed. Undo = hapus baris tersebut (bukan flag status). Sign-off ditolak dengan 409 kalau `actual_pct < 100` (BRD RULE-07).

**Generate Daily Task → Day Entry**: membuat Daily Task akan insert satu baris `day_entries` per tanggal kalender dalam rentang `[start_date, end_date]` inklusif, di dalam satu transaksi (SRS FR-DLY-01/02). `is_weekend` dihitung dari `entry_date` saat baca, tidak pernah disimpan.

**Day Entry bisa ditambah/dihapus manual (2026-08-10) — TIDAK LAGI dibatasi satu baris per tanggal.** `UNIQUE (daily_task_id, entry_date)` sudah DICABUT (migration `0010`). `POST /daily-tasks/{id}/day-entries` (body `{entry_date, planned_text?}`) nambah baris baru di tanggal manapun (dipakai dari tombol "➕" per baris — user mau breakdown lebih dari satu task di 1 hari yang sama); `DELETE /day-entries/{id}` hapus permanen (tombol "🗑" — mis. hari weekend/"lembur" yang ke-generate otomatis tapi PIC gak mau lembur). `actual_pct` dihitung dari SEMUA `day_entries` yang ADA saat dibaca — otomatis konsisten begitu baris ditambah/dihapus, tidak ada logic baru yang perlu disentuh (formula persisnya lihat poin `progress_pct` di bawah, berubah lagi di hari yang sama). `day_entries` list di-`ORDER BY entry_date, created_at` (kolom `created_at` baru ditambah migration `0010`) — SENGAJA bukan `updated_at`, karena `updated_at` berubah tiap PATCH (edit teks/status baris lama akan bikin urutannya lompat kalau dipakai buat sorting). Detail & alasan lengkap: `docs/decision-log/decision-log-day-entry-add-delete-20260810.md`.

**Day Entry status jadi 3-state granular (2026-08-10, migration `0011`) — `is_done` boolean DIGANTI `progress_pct` (smallint 0-100).** User minta progress lebih faktual: hari yang baru 90% dikerjain semestinya kontribusi 90% ke `actual_pct`, bukan dianggap 0% cuma karena belum "Selesai" penuh. 3 state (Belum/On Progress/Selesai) diturunkan MURNI dari `progress_pct` (`0`→Belum, `1-99`→On Progress, `100`→Selesai) — TIDAK disimpan sebagai kolom terpisah, ikuti pola computed-field yang sama kayak `verdict`/`expected_pct`. `actual_pct` di SEMUA level (Daily Task §5.1, Big Task §5.2, `board.Summary`, `weeklyplan.List`/`Push`) berubah dari `100*count(is_done)/count(*)` jadi `AVG(progress_pct)` — generalisasi murni, hasilnya identik kalau semua nilai masih 0/100. Frontend: `DayProgressStatus.svelte` (dropdown 3 pilihan + input angka 1-99 yang CUMA muncul kalau state = On Progress) gantiin `Toggle.svelte` di Day Entry — `Toggle.svelte` TETAP ADA buat boolean state lain yang genuinely dua-nilai, cuma gak dipakai lagi di sini. Logic turunan status (derive 3-state dari pct, resolve pct pas ganti dropdown, clamp input manual) diekstrak ke `lib/dayProgress.ts` (+ test) — pola sama kayak `dateRange.ts`/`comments.ts`. Detail & alasan lengkap: `docs/decision-log/decision-log-day-entry-progress-pct-20260810.md`.

**Auth**: JWT stateless untuk access token (HS256, env `JWT_SECRET`, expiry 2 jam) dikirim sebagai `Authorization: Bearer <token>` dari frontend. Refresh token JUGA JWT stateless terpisah (claim `typ:"refresh"`, expiry 7 hari), dikirim via cookie httpOnly (`Path=/api/v1/auth`, `SameSite=Lax`, `Secure` aktif kalau `APP_ENV=production`) — tidak ada tabel session/refresh-token di DB, sesuai `04-architecture.md` §6. Detail & alasan lengkap: `docs/decision-log/decision-log-auth-rbac-implementation-20260808.md`.

`auth.RequireAuth` (`backend/internal/auth/auth.go`) memvalidasi access token dan menyisipkan `userID` + `roles` (dari claim JWT, bukan query ulang ke DB) ke context. `auth.RequireRole("spv", ...)` adalah middleware terpisah yang harus dipasang SETELAH `RequireAuth` di chain — cek contoh pemasangannya di `main.go` untuk endpoint sign-off/undo-signoff. Role di access token statis selama masa berlaku token (maks 2 jam) — kalau role user diubah di tengah jalan, perubahan baru kepakai setelah access token expire dan di-refresh ulang (`POST /auth/refresh` query roles fresh dari DB tiap kali dipanggil).

Frontend: `frontend/src/lib/stores/authStore.ts` — access token disimpan in-memory saja (lewat `setAccessToken` di `client.ts`, bukan localStorage), sesi di-restore saat app dimuat lewat silent refresh (`POST /auth/refresh`, mengandalkan cookie httpOnly). Info user non-sensitif (nama/inisial/role/org_team) di-cache di `sessionStorage` cuma buat tampilan, bukan buat auth. Redirect ke `/login` ditangani reactive statement di `+layout.svelte`; redirect balik ke `/` setelah login dipanggil eksplisit di `login/+page.svelte`.

**Gotcha Svelte/SvelteKit (root layout gating) — PENTING, sempat regresi sekali:** di `+layout.svelte`, `<slot/>` HARUS selalu dibungkus div yang sama, di posisi {#if/else} yang sama, baik untuk state "halaman login" maupun "sudah login" — cuma `class` pada div pembungkus dan sibling-nya (titlebar/topbar) yang boleh toggle via `{#if}` terpisah. JANGAN taruh `<slot/>` sebagai satu-satunya isi cabang `{#if isLoginPage}` yang berbeda dari cabang `{:else}` tempat app-shell dirender — begitu perubahan state auth (abis login) terjadi bersamaan dengan navigasi route, `<slot/>` yang "pindah cabang" struktural bikin komponen halaman tujuan sempat mount DUA KALI (fetch data dua kali). Ketemu & diperbaiki di Fase 0 (wiring halaman login), lalu **regresi lagi** waktu redesign visual (Fase "1.5", adopsi mockup retro) karena struktur `{:else if isLoginPage} <slot/> {:else} <div class="app">...<slot/></div>` yang sama persis ditulis ulang tanpa sadar itu pola yang sama. Bentuk final yang aman ada di `routes/+layout.svelte` sekarang: satu `<div class="app" class:login-mode={isLoginPage}>` yang selalu ada, isinya `{#if !isLoginPage}` titlebar+topbar (tidak membungkus slot) lalu `<div class={isLoginPage ? 'login-content' : 'content'}><slot/></div>` — div pembungkus slot ini SELALU ada di posisi yang sama, cuma class-nya yang beda. Root cause pasti belum jelas (dugaan interaksi `<svelte:component>` remount SvelteKit dengan navigasi `goto()` di tick yang sama), tapi fix ini terbukti reprodusibel dua kali menghilangkan gejalanya — kalau nanti nambah state baru yang mengubah tampilan sekitar `<slot/>`, TIRU pola "div pembungkus tetap ada, cuma class/sibling yang toggle", jangan pola "cabang if terpisah yang isinya cuma slot".

**Role** bersifat many-to-many via `user_roles` (bukan kolom tunggal di `users`) — kode role: `spv`, `sa`, `dev`, `qa`, `admin` (di-seed di `0001_create_users_roles.up.sql`).

**Migration**: file SQL bernomor biasa di `backend/db/migrations/` (`NNNN_deskripsi.up.sql` / `.down.sql`), dijalankan via image Docker `migrate/migrate` (tidak perlu install CLI lokal) — lihat Makefile.

**Frontend — struktur halaman kerja (Fase 1):** `routes/+page.svelte` (Dashboard) menampilkan matriks summary per board (`GET /boards/{id}/summary`) buat cek cepat ala Vision Product §7 ("SPV jawab progress tim <1 menit"). `routes/boards/+page.svelte` adalah halaman kerja penuh: tab pilih board (state di URL via `?board=<id>`; form "+ Board baru" = nama + **deskripsi** — field `tag` lama dibuang, migration `0017`, `Board.description`) → `lib/components/BigTaskList.svelte` (list + create Big Task, tombol sign-off/undo-signoff, hanya tampil kalau role `spv` ada di `$auth.user.roles`) → expand baris → `lib/components/DailyTaskPanel.svelte` (list + create Daily Task, tabel Day Entry inline-editable, PIC picker dibatasi ke **anggota Big Task** via prop `members` — bukan semua user; lihat "Anggota Big Task" di bawah). `lib/types.ts` isinya bentuk response API (jangan hitung ulang field turunan di sini, selalu dari server). `lib/dateRange.ts` (+ `.test.ts`) adalah preview murni rentang tanggal buat form Daily Task, mencerminkan logika inklusif backend (SRS FR-DLY-01/02) — kalau backend-nya berubah, sinkronkan ini juga.

**Frontend — Users & Settings (Fase 2):** endpoint backend (`GET/POST /users`, `GET/PATCH /users/me`, `GET /users/assignable`, `GET /roles`) — lihat modul `user`. `authStore.init()` fallback ke `GET /users/me` kalau cache `sessionStorage` kosong; `auth.refreshUser()` dipanggil setelah `PATCH /users/me` sukses supaya topbar dkk ikut update. Detail keputusan bentuk endpoint (password wajib di `POST /users`, `GET /users/me` baru, `current_password` wajib buat ganti password, `GET /users/assignable` terpisah dari `GET /users`): `docs/decision-log/decision-log-users-api-gaps-20260809.md` dan `docs/decision-log/decision-log-pic-assignment-endpoint-20260809.md`. **UI-nya (My Profile / Settings) pindah jadi modal** — lihat bagian Design System di bawah, `routes/settings` dan `routes/users` (halaman) sudah dihapus.

## Design System (adopsi mockup, sesudah Fase 2)

Setelah Fase 0–2 selesai dengan styling polos, ditemukan `docs/rnd-ops-mockup_3.jsx` (2498 baris, React) — desain retro "Windows 9x/XP chrome" lengkap dengan 4 tema, sudah dikonfirmasi user sebagai **source of truth final**. Keputusan lengkap & alasan: `docs/decision-log/decision-log-design-system-adoption-20260809.md`. Semua halaman yang sudah ada (Login, Dashboard, Boards/BigTaskList/DailyTaskPanel) sudah di-re-skin mengikuti ini — **jangan bangun halaman baru dengan styling ad-hoc lagi, selalu pakai token & class di bawah.**

**PENTING — "retro" itu VISUAL doang, bukan wajib retro di semua interaksi (klarifikasi user, 2026-08-10).** Sempat 2 kali ke-drift ke arah "murni retro": (1) `.app` dikasih `max-width` fixed + gray desktop backdrop biar kelihatan kayak window ngambang — DITOLAK user, lihat gotcha layout di bagian Arsitektur; (2) semua boolean state (mis. Day Entry selesai/belum) dipaksa jadi tombol teks gonta-ganti label. Rule-nya: token warna/font/border/chrome (titlebar, tabs, badge, dst) tetap ikut retro theme, TAPI kalau ada pola interaksi MODERN yang lebih jelas buat user (toggle switch buat boolean, dst), pakai itu — jangan maksa niru semua widget Windows 9x lawas cuma demi konsistensi tema. Contoh yang sudah dibuat: `Toggle.svelte` (`.toggle-switch`, pill switch modern) sempat gantiin `.day-status-btn` lama buat status Day Entry, tapi Day Entry sendiri berubah lagi tak lama setelahnya jadi 3-state (`DayProgressStatus.svelte`, lihat bagian Weekly Plan/Daily Task di atas) — `Toggle.svelte` tetap dipertahankan buat boolean state lain yang genuinely dua-nilai. Kalau ragu "ini masih dalam batas retro-visual atau udah kejauhan", tanya user, jangan asumsi sepihak seperti gotcha #1 di atas.

**Token & komponen dasar** (`frontend/src/app.css`): custom properties per tema didefinisikan lewat `:root[data-theme="retro-light|retro-dark|modern-light|modern-dark"]` (bukan inline `<style>` dengan JS template literal seperti mockup aslinya — di-port ke CSS murni). Named vars: `--face`, `--content-bg`, `--content-alt`, `--text-primary`, `--text-muted`, `--titlebar-a/b`, `--win-blue/-light`, `--win-green`, `--win-red`, `--win-amber`. Class dasar yang sudah di-port: `.titlebar`, `.topbar`/`.tabs`/`.tab-btn`, `.stat-card`/`.stat-row`, `.badge`+`.badge-{verdict}`, `.dualbar`, `.sheet-table`, `.section`/`.section-title`, `.board-pills`/`.board-pill`, `.project-layout`/`.bigtask-list`/`.bigtask-detail`, `.daily-task-card`/`.daily-day-table`/`.toggle-switch`/`.day-progress` (lihat catatan retro-vs-modern di atas), `.inline-input`/`.inline-form`, `.quick-btn`/`.add-card-ghost`/`.sign-btn`, `.overlay`/`.panel`/`.modal-box`, `.dropdown-panel`, `.comment-section`/`.cheatsheet-section`, `.reviewer-picker`/`.reviewer-avatars`, `.week-nav`. Font: `Tahoma, 'MS Sans Serif', 'Segoe UI', Arial, sans-serif`, ukuran dasar 11px — SEMUA komponen baru wajib ikut ini, jangan bikin ukuran font/spacing sendiri.

**Tema disimpan di `users.theme_preference`** (backend, sudah ada dari Fase 2), state UI-nya di `lib/stores/themeStore.ts` (`theme` writable + array `THEMES` buat render pilihan). `+layout.svelte` sinkronkan `$auth.user.theme_preference` → `theme` store → `document.documentElement.setAttribute('data-theme', ...)`. Ganti tema = `SettingsModal` panggil `PATCH /users/me {theme_preference}` lalu `auth.refreshUser()`.

**Komponen reusable** (`lib/components/`): `Avatar.svelte` (props `initials`, `size`), `VerdictBadge.svelte` (props `verdict`), `DualBar.svelte` (props `expected`, `actual`), `StatCard.svelte` (props `label`, `value`, `tone?`), `DonutChart.svelte` dan `GroupedBarChart.svelte` — **chart custom SVG, BUKAN library** (mockup pakai Recharts/React, tidak ada versi Svelte resmi; kebutuhan chart-nya simpel jadi tidak worth nambah dependency baru — lihat decision log). Kalau butuh chart baru yang jauh lebih kompleks dari donut/grouped-bar, itu baru alasan valid buat evaluasi ulang keputusan ini.

**Dashboard lintas board, POV per BOARD (=project) bukan per Big Task.** Diagregasi di CLIENT dari `GET /boards` + `GET /boards/{id}/big-tasks` tiap board (di-`Promise.all`), TIDAK ada endpoint backend baru buat ini. Skala kecil (NFR-03) bikin ini aman performa. Sempat SALAH sekali (regresi ditemukan user via screenshot 2026-08-10): stat card/donut/chart/tabel sempat menghitung per Big Task langsung, padahal Dashboard-nya dimaksudkan buat lihat progress per **project** (Vision Product §7) — board = project. Fix & rule lengkap: `docs/decision-log/decision-log-dashboard-board-level-aggregation-20260810.md`. Agregasi 2 tingkat di `lib/dashboardStats.ts`: `aggregateBoards()` (Big Task → satu baris `BoardAgg` per board) lalu `computeDashboardStats()` (BoardAgg[] → statistik portfolio). Rule status board: **all-or-nothing per bucket, default "running" kalau campuran** — `done` HANYA kalau SEMUA Big Task signed (match `project_status` di `GET /boards/{id}/summary`), `hold` HANYA kalau SEMUA on_hold, `not_started` HANYA kalau SEMUA 0%/board belum punya Big Task; campuran apapun → `running`. Rule verdict board: **asimetris** dengan status (ikut filosofi netral-sampai-keputusan yang sama dengan verdict Big Task individual) — `lose` kalau ADA MINIMAL SATU Big Task lose (sinyal negatif tidak boleh ketutupan rata-rata, walau status board-nya `done`), `won` HANYA kalau status `done` DAN tidak ada lose sama sekali. Progress chart pakai rata-rata `actual_pct`/`expected_pct` semua Big Task di board; "Deadline terdekat" pakai `min(days_left)` Big Task yang belum signed. Kalau nambah metrik dashboard baru, agregasi dulu ke level board pakai pola ini, JANGAN render satu baris per Big Task langsung.

**Gotcha/bug ketemu 2026-08-10 — section "Progress: actual vs expected (%)" + "Deadline terdekat" cuma nampilin board `status==='running'`, jadi board baru dibuat (0 Big Task) TIDAK PERNAH muncul di situ** (padahal sudah benar muncul di stat card/donut "Status project" — cuma section ini yang ke-skip). User laporkan lewat reproduksi langsung (bikin board kosong, cek Dashboard). Fix: field `activeBoards` (BUKAN `runningBoards` lagi) = SEMUA board KECUALI `status==='done'` — jadi `not_started` dan `hold` ikut muncul (0% vs 0%/target buat yang belum mulai), cuma board yang udah "Selesai" (semua Big Task sign-off) yang dikeluarkan (sudah cukup terwakili di stat card, gak perlu dipantau progress-nya lagi). Dipakai konsisten di `progressChartData`, attention-list, DAN `nearestDeadline` (satu aturan, bukan beda-beda per section) — lihat `docs/decision-log/decision-log-dashboard-progress-scope-20260810.md`. Kalau nambah section dashboard baru yang nge-list board, pikirkan dulu status mana yang relevan ditampilkan — jangan default ke "cuma yang `running`" tanpa alasan eksplisit. "Tim" grid di Dashboard cuma nampilin identitas+role dari `GET /users/assignable` — TIDAK ada hitungan "task aktif"/"belum direview" per orang (butuh cross-reference Daily Task lintas board yang mahal utk N+1 fetch, dan "belum direview" butuh Review Queue yang belum dibangun/Fase 7) — jangan tambah balik tanpa endpoint agregat backend yang proper.

**My Profile & Settings jadi modal**, bukan route halaman (`lib/components/ProfileModal.svelte`, `lib/components/SettingsModal.svelte`), dipanggil dari dropdown avatar user di topbar (`+layout.svelte`) — sesuai pola asli mockup (bukan cuma warnanya yang diikuti, pola interaksinya juga). `SettingsModal` punya 2 tab: "Manajemen user" (cuma render kalau admin/spv) dan "Tema aplikasi" (semua role).

**Gotcha layout: `.app` HARUS fluid (ikut lebar browser), JANGAN dikasih `max-width` + `margin:auto`.** Sempat dilaporkan user "My Weekly Plan berantakan" di monitor lebar (2026-08-10, sesi pertama) — waktu itu di-"fix" dengan `.app { max-width: 1280px; margin: 0 auto; }` + `body{background:#808080}`, biar kelihatan kayak window retro ngambang di desktop abu-abu. User COMPLAIN LAGI setelahnya: itu bikin app kelihatan "murni retro"/fixed, gak ngikutin lebar browser — padahal maunya tema retro cuma VISUAL (warna/font/border), bukan literal window fixed-width. **Fix final**: `.app` fluid (`width:100%`, TANPA max-width/margin-auto/border-outset-window), root cause sparse-table di-fix DI TABEL-nya sendiri (`.weekplan-table { max-width: 900px; }` di `app.css`), bukan di container `.app`. Kalau nambah tabel/section baru yang kolomnya dikit dan berantakan di monitor lebar, kasih `max-width` ke ELEMEN ITU SENDIRI, jangan pernah cap `.app`.

**Gotcha: `<input type="date">` dua-berdampingan di sidebar sempit (200px, `.bigtask-list`) overflow keluar container.** Input date native punya minimum rendered width intrinsik yang lebih lebar dari separuh 200px, jadi form "Tambah big task" (`BigTaskList.svelte`, pakai `.inline-form-dates`) date picker-nya "keluar" dari sidebar kalau cuma `display:flex` polos tanpa wrap. Fix: `.inline-form-dates` dapat `flex-wrap: wrap; width: 100%;` + child (`.inline-input`, `input[type='date']`, `> label`) dapat `flex: 1 1 100px; min-width: 0;` — biar shrink dan jatuh ke baris baru kalau sempit, bukan overflow. Class ini dipakai bareng oleh `BigTaskList.svelte` (tanpa label wrapper) dan `DailyTaskPanel.svelte` (dengan `<label>` wrapper) — kalau nambah form tanggal baru di kolom sempit, pakai class ini, jangan bikin flex row date input baru dari nol.

**Gotcha overlay/modal + a11y linter Svelte:** div overlay yang `on:click` buat nutup modal (klik backdrop) akan selalu kena warning `a11y-click-events-have-key-events` dan `a11y-no-static-element-interactions`/`a11y-no-noninteractive-element-interactions` — solusinya bukan `role="presentation"` (itu malah bikin div-nya dianggap non-interactive dan warning JUSTRU muncul lagi di elemen yang sama, konflik sama listener yang menetap di elemen itu), tapi kombinasi `<svelte:window on:keydown={escHandler}/>` buat Escape + `<!-- svelte-ignore a11y-click-events-have-key-events -->` + `<!-- svelte-ignore a11y-no-static-element-interactions -->` di atas div overlay, DAN `<!-- svelte-ignore a11y-no-noninteractive-element-interactions -->` tambahan di div `modal-box` bagian dalam yang punya `on:click|stopPropagation` (kena rule berbeda dari overlay-nya). Lihat `ProfileModal.svelte`/`SettingsModal.svelte` buat contoh lengkapnya.

**Gotcha: parent/child staleness setelah update anak.** `BigTaskList` fetch sendiri data agregat Big Task (actual_pct/verdict/eligibility sign-off); `DailyTaskPanel` (anak, di-render saat expand) fetch data Daily Task-nya sendiri. Update di anak (toggle Day Entry, create Daily Task) TIDAK otomatis me-refresh data parent — kalau tidak ditangani, header Big Task & tombol sign-off jadi basi (sempat 409 padahal progress sebenarnya sudah 100%, ketemu & diverifikasi langsung waktu wiring Fase 1). Solusi yang dipakai: `DailyTaskPanel` `dispatch('updated')` (Svelte `createEventDispatcher`) tiap kali sukses create/update, parent dengerin via `on:updated={() => load({ silent: true })}` (lihat gotcha `silent` di bawah — JANGAN `on:updated={load}` polos). Kalau nambah level nesting baru (mis. komentar di bawah Daily Task), pola yang sama harus diikuti — jangan asumsikan re-fetch otomatis lintas komponen.

**Gotcha: re-fetch setelah mutasi kecil (edit 1 field) terasa kayak "page refresh" walau SPA — WAJIB pola `load({ silent })`.** Dilaporkan user 2026-08-10: ubah teks "Rencana" di Day Entry (tersimpan via `on:change`, fire saat blur) kerasa kayak submit HTML form biasa, bukan reactive update senyap ala Vue/React. Root cause BUKAN soal Svelte vs framework lain — murni bug pola loading state: `load()` di semua komponen list (`DailyTaskPanel`, `BigTaskList`, `CheatSheetSection`, `CommentSection`, `weekly-plan/+page.svelte`) toggle `loading = true` di awal SETIAP kali dipanggil, termasuk dipanggil ulang setelah mutasi kecil (PATCH day-entry, post comment, dst) — karena template-nya `{#if loading} <p>Memuat...</p> {:else} ...seluruh list/tabel... {/if}`, toggle itu bikin SELURUH subtree (semua input, termasuk yang lagi difokus) unmount lalu remount tiap 1 field disimpan. Lebih parah lagi: `BigTaskList` dengerin event `updated` dari `DailyTaskPanel` anaknya dan ikut manggil `load()` juga — jadi edit 1 field Day Entry ikut nge-flash SELURUH `project-layout` (termasuk `DailyTaskPanel`-nya sendiri lewat `{#key activeBt.id}`). **Fix**: semua fungsi `load()` yang dipanggil ulang setelah mutasi kecil sekarang terima parameter `{ silent = false }` — `if (!silent) loading = true/false`, dan SEMUA caller pasca-mutasi (bukan initial mount, bukan ganti board/minggu) pakai `load({ silent: true })`. Svelte keyed `{#each ... (item.id)}` yang sudah dipakai di semua list ini otomatis nge-update DOM in-place tanpa destroy elemen begitu `loading` gak lagi di-toggle — jadi persis reactive update ala Vue/React, tanpa perlu ubah struktur `{#each}`-nya sama sekali. Kalau nambah komponen baru dengan pola `load()` + reload-setelah-mutasi, WAJIB ikut pola `{ silent }` ini dari awal, jangan sampai kejadian lagi.

**Gotcha Svelte (reactive assignment vs `bind:value`):** JANGAN pernah tulis `$: someVar = $store.field` untuk variabel yang juga dipakai di `bind:value={someVar}` pada input yang sama. Reactive statement itu re-run tiap invalidation cycle (termasuk yang dipicu oleh `bind:value`-nya sendiri) dan langsung menimpa balik apa pun yang baru diketik user — ketemu & diverifikasi di `settings/+page.svelte` (form ganti nama sempat tidak bisa disimpan sama sekali karena reactive statement terus me-reset input ke nilai lama). Fix: init pakai `let someVar = $store.field` biasa (sekali saja, non-reaktif), bukan `$:`. Pola ini berlaku di mana pun ada form yang initial value-nya dari store/props tapi field-nya user-editable.

**Gotcha: response 200 tanpa body itu nyata, client harus defensif.** `bigtask.SignOff` awalnya cuma `w.WriteHeader(http.StatusOK)` padahal `05-api-contract.md` §4 bilang harus balikin Big Task object — `res.json()` di frontend pada body kosong throw "Unexpected end of JSON input", bikin sign-off yang SUKSES di backend kelihatan GAGAL di frontend (silent contract violation, ketemu pas wiring Fase 1, sudah diperbaiki: `bigtask.loadBigTask` helper dipakai buat balikin object lengkap). `lib/api/client.ts` sekarang baca response sebagai teks dulu sebelum `JSON.parse` (bukan langsung `res.json()`), biar deviasi kontrak serupa di endpoint lain nggak bikin gagal-diam-diam — tapi backend tetap harus diperbaiki juga tiap kali ketemu kasus begini, defensive parsing ini cuma jaring pengaman kedua.

**Comments (Fase 3):** `backend/internal/comment/handler.go` — `GET /big-tasks/{id}/comments?scope=all|general|{daily_task_id}` dan `POST` (author_id selalu dari JWT, FR-CMT-05). Frontend: `lib/components/CommentSection.svelte`, dirender di `BigTaskList.svelte` setelah `DailyTaskPanel` untuk Big Task yang aktif. `DailyTaskPanel` yang punya daftar Daily Task asli (buat label scope filter) — dikirim ke atas via event `tasksLoaded` (bukan CommentSection fetch sendiri), dan tombol "💬 Komentar" per Daily Task dispatch `jumpToComment` yang diteruskan `BigTaskList` sebagai prop `jumpScope`/`jumpToken` ke `CommentSection` (pola sama seperti `jumpScope`/`jumpToken` di `ProjectView` mockup — `jumpToken` di-increment tiap klik supaya reactive block ke-trigger walau `jumpScope`-nya sama seperti klik sebelumnya).

**Render komentar wajib lewat `lib/comments.ts` (`renderCommentHtml`), jangan `{@html}` body mentah.** Body komentar itu user input, di-escape HTML dulu baru mention (`@Nama` yang match `display_name` assignable user) dibungkus `<span class="mention-tag">` — escape SELALU jalan duluan sebelum wrap mention, urutan ini yang bikin aman dari XSS (kalau dibalik, span yang disisipkan bisa ke-escape juga atau body-nya keburu jadi vector). Ada unit test di `comments.test.ts` termasuk kasus body berisi tag HTML.

**`bigtask.computeExpectedPct`** (fungsi murni, ada test-nya) dipakai bareng oleh `ListByBoard` dan `loadBigTask` — kalau nambah endpoint baru yang butuh bentuk Big Task lengkap (mis. modul lain yang merujuk balik ke Big Task), pakai `loadBigTask`/`computeExpectedPct`, jangan copy-paste query+kalkulasinya lagi.

**Cheat Sheet & Upload (Fase 4):** `backend/internal/cheatsheet/` — CRUD referensi board (`file`/`url`/`note`), scope-nya BOARD bukan Big Task (beda dari comments). `backend/internal/upload/` — modul terpisah, `POST /uploads` (multipart, field `file`) simpan ke disk lokal (`UPLOAD_DIR`, default `/app/uploads`, di-mount sebagai docker volume `uploads` — **wajib ada volume-nya**, tanpa itu file hilang tiap restart container) dengan nama `<uuid>_<nama-asli-disanitasi>` (cegah collision & path traversal), dan `GET /uploads/{filename}` buat ambil lagi. Endpoint `GET /uploads/{filename}` tidak ada di draft awal kontrak — ditambahkan karena tanpa itu upload tidak bisa dipakai lagi (lihat `docs/decision-log/decision-log-file-upload-storage-20260809.md`).

**Gotcha: link download file TIDAK BOLEH pakai `<a href>` biasa.** Semua endpoint butuh `Authorization: Bearer <token>`, tapi browser tidak menempelkan header custom ke navigasi `<a href>` biasa (cuma cookie yang otomatis terkirim) — access token kita disimpan in-memory, bukan cookie, jadi `<a href="/api/v1/uploads/...">` akan SELALU 401 di real usage. Fix: `lib/api/client.ts` punya `downloadBlob(path)` yang fetch manual dengan header Authorization lalu dikembalikan sebagai `Blob`; caller (`CheatSheetSection.svelte`) bikin object URL sementara dan trigger `<a>` sintetis via JS buat memicu download beneran, nama file asli (`originalFilename()`, strip prefix UUID) dipakai sebagai `download` attribute. Pola ini WAJIB diikuti untuk resource lain yang butuh auth dan didownload langsung oleh browser (bukan di-fetch lalu ditampilkan sebagai JSON).

**Frontend Cheat Sheet**: `lib/components/CheatSheetSection.svelte`, dirender di `BigTaskList.svelte` di ATAS `project-layout` (scope board, tampil sekali per board terlepas Big Task mana yang aktif — beda dari `CommentSection` yang di dalam `bigtask-detail`). Upload pakai `api.upload()` (helper baru di `client.ts`, `FormData` sebagai body, TIDAK boleh set `Content-Type` manual — biarkan browser yang isi header multipart boundary-nya sendiri) sebelum `POST /boards/{id}/cheat-sheet` dengan `value` hasil upload.

**Clone-as-review (Fase 5; DIUBAH 2026-08-11 — by orang, bukan role):** `dailytask.CloneReview` — `POST /daily-tasks/{id}/clone-review` body `{reviewer_user_id, start_date, end_date}`, bikin Daily Task baru beneran di server. Reviewer WAJIB anggota Big Task (400 kalau bukan). Judul otomatis `"[Review {display_name}] {judul asal}"` (nama spesifik, bukan role), PIC = reviewer itu, dan menyimpan `review_of_daily_task_id` = daily task asal (dipakai Review Queue). `insertDailyTaskWithDays` sekarang terima param `reviewOf *string` (nil buat Create biasa). Frontend: satu tombol "+ Review" di `daily-task-head-right` (`DailyTaskPanel.svelte`) buka form inline: dropdown reviewer (dari prop `members`) + tanggal + preview judul. **`decision-log-clone-review-20260809.md` (pemilihan role SPV/QA) DIGANTIKAN** oleh `decision-log-bigtask-members-refactor-20260811.md`.

**`dailytask.loadDailyTask`/`insertDailyTaskWithDays`** (helper, pola sama seperti `bigtask.loadBigTask`) dipakai bareng oleh `Create` dan `CloneReview` — kalau nambah cara baru bikin Daily Task, pakai helper ini, jangan copy-paste query insert+day-entries-loop lagi. Sekalian nemu & benerin bug lama di sini: `Create` awalnya cuma balikin `{"id": ...}`, padahal kontrak minta full Daily Task object (mirip pola bug `SignOff` di Fase 1) — sekarang keduanya balikin object lengkap via `loadDailyTask`.

**Weekly Plan (Fase 6):** `backend/internal/weeklyplan/` — `GET /weekly-plan?week_start=YYYY-MM-DD` (rollup lintas board, cuma Big Task yang punya minimal satu Day Entry di rentang minggu itu yang muncul — dijamin lewat INNER JOIN `day_entries` yang di-filter di kondisi JOIN, bukan di `WHERE` setelahnya) dan `POST /weekly-plan/push` (upsert `weekly_push_log` via `ON CONFLICT (big_task_id, week_start)` — `callback_id` cuma digenerate sekali, push berikutnya cuma update `pushed_at`+snapshot). Push HR ke `weekly_push_log` SELALU jalan (sumber kebenaran lokal); integrasi HR eksternal beneran (best-effort) lihat poin `myagenda-service` di bawah.

**Integrasi HR — `myagenda-service` (mulai 2026-08-10, dummy/lokal, sub-project TERPISAH):** `myagenda-service/` di root repo — Go module, docker-compose, Makefile SENDIRI (BUKAN bagian dari `docker-compose(.dev).yml` rnd-task-board) karena production-nya nanti dipasang LANGSUNG di server aplikasi HR (di luar repo ini), bukan di-deploy bareng rnd-task-board. Skema `my_agenda` (MySQL) DIPAKAI PERSIS DDL yang dikasih user — tabel milik sistem HR asli, TIDAK BOLEH diubah (termasuk nambah unique constraint) walau DDL-nya cuma punya PK auto-increment. `POST /my-agenda` upsert APLIKASI-LEVEL berdasarkan `(user_id, judul_task, tgl_rencana)` sebagai identitas natural (SELECT dulu, lalu INSERT/UPDATE — bukan `ON DUPLICATE KEY` SQL, karena gak ada unique index buat itu). `weeklyplan.Push` (backend utama) manggil service ini via HTTP SETELAH `weekly_push_log` sukses — BEST-EFFORT, `MYAGENDA_SERVICE_URL` kosong/gagal connect TIDAK membatalkan response 200 ke user, cuma di-log (lihat `pushToMyAgenda`/`placeholderHRUserID` di `backend/internal/weeklyplan/handler.go`). **`user_id` yang dikirim MASIH PLACEHOLDER** (`placeholderHRUserID`: CRC32 dari UUID user kita, BUKAN id pegawai HR asli — belum ada tabel mapping) — WAJIB diganti sebelum production nyata, butuh input tim HR/System Analyst. Di `docker-compose.dev.yml`, backend rnd-task-board reach service ini lewat `host.docker.internal` (beda docker network, bukan nama service) — `extra_hosts: host-gateway` sudah diset. Detail & alasan lengkap: `docs/decision-log/decision-log-myagenda-hr-service-20260810.md`, cara jalanin: `myagenda-service/README.md`.

## Claude Chat Bridge — `claude-chat-service` (2026-08-11, sudah diimplementasi)

Sub-project di root repo, Node.js/TypeScript + **Claude Agent SDK** (bukan shell-out ke CLI mentah), docker-compose/`package.json`/README sendiri — konsisten sama precedent `myagenda-service`. Keputusan arsitektur lengkap: `docs/decision-log/decision-log-claude-chat-service-20260811.md`. Referensi endpoint & WS schema: `claude-chat-service/docs/api_contract.md`. Panduan integrasi consumer: `claude-chat-service/docs/integrations_guide.md`.

**Key files:**

| File | Fungsi |
|---|---|
| `src/index.ts` | Entry point — Express app + HTTP server + attachChatSocket |
| `src/sessionManager.ts` | `ChatSession` class + CRUD session (createSession/getSession/closeSession/listSessions) |
| `src/ws/chatSocket.ts` | WS upgrade handler `/ws/sessions/:id` — fan-out SDK events ke semua WS client |
| `src/routes/sessions.ts` | REST CRUD `/sessions` |
| `src/routes/fs.ts` | `GET /fs/browse` — directory picker sebelum buat session |
| `src/routes/auth.ts` | SSE `POST /auth/setup-token` + `GET /auth/status` |
| `src/auth/setupTokenBridge.ts` | PTY bridge ke `claude setup-token` CLI |
| `src/auth/tokenStore.ts` | Simpan/load OAuth token ke disk (`TOKEN_STORE_PATH`) |
| `src/lib/asyncQueue.ts` | `AsyncQueue<T>` — push-based AsyncIterable buat multi-turn tanpa respawn |
| `src/lib/cwdValidation.ts` | `validateCwd()` — tolak home/root/forbidden paths (pure fn, ada unit test) |
| `src/types.ts` | `CreateSessionRequest`, `SessionSummary`, `ClientToServerMessage`, `ServerToClientMessage` |

**Ringkasan keputusan kunci** (jangan implementasi menyimpang dari ini tanpa diskusi ulang):
- **Deployment**: server bersama, SATU akun Claude subscription (Pro/Max, OAuth) dipakai bersama semua sesi — bukan API key billing.
- **TIDAK ADA RBAC/auth di service ini sendiri** (sengaja, sementara) — full trust ke pemanggil, otorisasi didelegasikan ke `rnd-task-board` backend. Auth peer-to-peer antar service DITUNDA ke iterasi berikutnya, bukan diabaikan permanen.
- **`cwd` wajib dipilih eksplisit oleh user sebelum chat mulai** — `GET /fs/browse` untuk navigasi direktori, `POST /sessions` yang nerapkan validasi ketat (tolak `$HOME`/root/`CWD_FORBIDDEN_ROOTS`).
- **Session manager internal** — `ChatSession` bungkus satu `Query` SDK yang hidup terus (via `AsyncQueue` sebagai streaming input), bukan spawn ulang tiap prompt. `set_mode`/`set_model`/`interrupt` bisa dipanggil live di session yang sama.
- **Resume** = field `resume` di `POST /sessions` (bukan endpoint terpisah) — bikin session internal baru yang bungkus riwayat Claude lama via `claude_session_id`.
- **Login BUKAN flow per-chat-session** — provisioning kredensial pakai `claude setup-token` via PTY (`POST /auth/setup-token` SSE). Token OAuth ~1 tahun, di-load otomatis tiap service start dari `TOKEN_STORE_PATH`.
- **`total_cost_usd` in-memory** — diakumulasi dari event `result` SDK, reset tiap service restart. Belum ada persistent storage.

**Gotcha: `extractToken()` di `src/auth/setupTokenBridge.ts` BELUM divalidasi terhadap output CLI asli** — sengaja tidak dijalankan selama development (efek nyata: generate OAuth token baru). Verifikasi/sesuaikan regex parsing pas dites langsung pertama kali.

**Cara `rnd-task-board` konek ke service ini SUDAH diputuskan (2026-08-11): backend proxy (Opsi B).** Lihat bagian "Change Request §6" di bawah + `decision-log-change-request-integration-20260811.md`. **Masih ditunda:** auth peer-to-peer `rnd-task-board` ↔ `claude-chat-service` (service tetap full-trust; proxy jadi satu-satunya gerbang auth, cukup karena service cuma di-reach via proxy di jaringan internal), dan tempat penyimpanan akumulasi cost yang persist (masih in-memory di service).

## Change Request §6 — Integrasi claude-chat-service (2026-08-11)

Vision Product §6 (Mekanisme Evolusi Produk): anggota tim mengusulkan perubahan lewat percakapan bebas (dibantu Claude Agent SDK di `claude-chat-service`), disusun jadi draft `change_request.md`, ditriase SPV & SA. Fondasi service-nya lihat bagian "Claude Chat Bridge" di atas; integrasi ke portal ini didetailkan di `decision-log-change-request-integration-20260811.md`.

**Koneksi = backend proxy (Opsi B).** `backend/internal/chatproxy/` mem-reverse-proxy `/api/v1/chat/*` → `CLAUDE_CHAT_SERVICE_URL` (default `http://localhost:8090`; di dev compose diarahkan ke `host.docker.internal:8091` — sengaja BEDA port dari myagenda `:8090` biar gak tabrakan kalau dua-duanya jalan di host). Frontend TIDAK pernah connect langsung ke service; cukup tahu `/api/v1`. `httputil.ReverseProxy` yang sama menangani REST DAN WebSocket (Go handle `Connection: Upgrade` sebagai tunnel).

**Gotcha auth WS — token via query param, bukan header.** Browser `WebSocket` TIDAK bisa nyetel header `Authorization`, dan access token kita in-memory (bukan cookie) — jadi proxy nerima token WS lewat `?access_token=` (di-strip sebelum diteruskan ke service). REST tetap pakai header `Authorization: Bearer`. Keduanya divalidasi di proxy via `auth.ParseAccessToken` (fungsi baru yang diekstrak dari `RequireAuth`, dipakai bareng, ada test — security-critical). Proxy dipasang DI LUAR grup `protected` di `main.go` (karena `RequireAuth` cuma baca header) dan melakukan auth sendiri (header ATAU query). Ini pola yang sama filosofinya dgn gotcha `downloadBlob` (resource ber-auth yang di-reach browser langsung butuh mekanisme khusus, bukan `<a href>`/WS polos). Frontend: `lib/chatClient.ts` (`buildChatWsUrl` pure + tested, `openChatSocket`), `getAccessToken()` baru di `client.ts`. `vite.config.ts` proxy `/api` sekarang `{ ws: true }` — WAJIB, kalau tidak WS gak diproksikan ke backend.

**Intercept lokal `GET /api/v1/chat/local/config`** — TIDAK diteruskan ke service, balikin `{default_cwd}` dari env `CHAT_DEFAULT_CWD` (didokumentasikan di `backend/.env.example`; di dev = path repo). Endpoint lain diteruskan apa adanya (prefix `/api/v1/chat` di-strip: `/sessions`, `/fs/browse`, `/ws/sessions/:id`).

**Direktori sesi = dari config backend, BUKAN dipilih user.** `CHAT_DEFAULT_CWD` di-set sekali di `.env` backend (path deployment tempat `claude-chat-service` baca repo) — frontend nampilin read-only + langsung "Mulai percakapan", gak ada friksi milih path. Dir picker (`/fs/browse`) cuma FALLBACK kalau env belum di-set (config balikin string kosong). Kalau `CLAUDE_CHAT_SERVICE_URL` kosong, proxy default ke `localhost:8090` yang DI DALAM container backend = dirinya sendiri → 502 (gejala yang sempat kejadian pas container belum di-recreate abis nambah env; recreate `docker compose -f docker-compose.dev.yml up -d --no-deps backend`).

**Permission mode chat change-request = `plan` (read-only) — SENGAJA.** Assistant boleh BACA repo (biar usulan kontekstual) tapi tidak menulis/eksekusi. Draft `change_request.md` dihasilkan sebagai TEKS di percakapan, lalu disimpan aplikasi ke `change_requests.raw_conversation` (bukan assistant nulis file). Alasan: service masih no-auth (p2p auth ditunda) → batasi blast radius; §6 tujuannya nyusun dokumen buat ditriase manusia, bukan langsung ngoding. JANGAN naikin ke `bypassPermissions` dari fitur ini tanpa auth p2p dulu.

**`change_requests` (tabel dari migration `0006`, akhirnya dipakai UI):** `backend/internal/changerequest/` — `GET /change-requests` (list, SEMUA user login — §6 "seluruh anggota tim"), `POST /change-requests` (submit, `submitted_by` dari JWT, status awal `pending`), `PATCH /change-requests/{id}` (triase, digate `RequireRole("spv","sa")` — set status + `reviewed_by`/`reviewed_at`; balik ke `pending` bersihin jejak reviewer). `isValidStatusTransition` fungsi murni + test (semua transisi antar 4 enum diizinkan; disiapkan buat diperketat nanti). Frontend: `routes/change-requests/+page.svelte` (list + triase kalau `spv`/`sa`) + `lib/components/ChangeRequestChat.svelte` (cwd dari config → sesi plan → streaming WS → tombol "Susun change request" kirim prompt kanonik → "Simpan"). Logic murni ke-extract & di-test: `lib/chatMessages.ts` (`parseSdkMessage`/`mergeAssistantText`), `lib/chatClient.ts` (`buildChatWsUrl`), `lib/changeRequest.ts` (`crStatusLabel`/`crStatusTone`). Badge tone generik baru di `app.css`: `.badge-good/-bad/-accent/-neutral` (bukan verdict-specific).

**`document_md` terpisah dari `raw_conversation` (2026-08-12) — rename kolom `structured_doc_path` lama** (ada dari migration `0006`, TIDAK PERNAH dipakai — niat awal path file di disk, gak pernah dibangun) **jadi `document_md`** (migration `0021`), isinya markdown INLINE (bukan path file, konsisten sama `raw_conversation`). Diisi otomatis di `chatSessionStore.ts`: `compile()` set flag `awaitingCompileTurn`, begitu event `result` SDK turn abis "Susun change request" itu selesai, teks balasan assistant terakhir disalin ke state `compiledDocument` — TERPISAH dari `messages`/transcript penuh. `saveChangeRequest()` kirim keduanya (`raw_conversation` = seluruh transcript via `buildTranscript()`, `document_md` = `compiledDocument`, null kalau user belum pernah klik "Susun change request"). Frontend list (`routes/change-requests/+page.svelte`) sekarang punya DUA toggle independen per item: "Lihat percakapan" (raw_conversation) dan "Lihat dokumen" (document_md, tombolnya CUMA muncul kalau field-nya terisi) — keduanya dirender via `renderMarkdown()` + class `.cr-md`, BUKAN `<pre>` plain text lagi (sebelumnya transcript kebaca mentah dengan karakter `##`/`**` literal, dikeluhkan user). **`.cr-md` (style markdown viewer) DIPINDAH dari scoped style `ChangeRequestChat.svelte` ke `app.css` global** — dipakai lintas komponen (bubble chat DAN list page), Svelte scoped style gak nembus antar komponen. Gotcha flexbox ketemu di sini: `.cr-item` (flex child `.cr-list` yang `display:flex`) butuh `min-width:0` eksplisit, kalau tidak browser biarkan item melebar ngikutin baris terpanjang alih-alih teksnya yang wrap (`min-width:auto` default flex item) — pola ini berlaku di mana pun ada teks panjang/dinamis di dalam flex container, bukan cuma di sini. Detail: `docs/decision-log/decision-log-change-request-document-20260812.md`.

**`claude-chat-service` DIJALANKAN DI HOST, bukan via docker-compose-nya sendiri (untuk dev lokal).** Alasan: (a) service baca direktori project langsung dari filesystem — di host, `CHAT_DEFAULT_CWD=/home/lugidev/rnd-task-board` langsung valid; kalau di Docker harus mount path & cwd jadi `/workspaces/...` (gak match config); (b) auth OAuth — `claude` CLI di host sudah authenticated (login user), jadi Agent SDK pakai kredensial itu LANGSUNG tanpa perlu `POST /auth/setup-token` (dikonfirmasi `GET /auth/status` → `loggedIn:true`). `docker-compose.yml` di `claude-chat-service/` itu buat skenario deploy lain (server HR). Cara jalanin dev: `cd claude-chat-service && PORT=8091 node dist/index.js` (atau `npm run dev`) — PORT 8091 sengaja beda dari myagenda `:8090`. Backend (di Docker) reach via `host.docker.internal:8091` (Node `listen(port)` default bind semua interface, jadi reachable dari container). Sudah diverifikasi FULL end-to-end live 2026-08-11: browser → proxy WS (`?access_token=`) → service → Claude SDK → balasan asli, `result:success`.

**Auto-init sapaan saat sesi mulai (2026-08-11):** klik "＋ Ajukan usulan" → kalau `CHAT_DEFAULT_CWD` sudah di-set, `beginSetup()` LANGSUNG `startSession()` (skip layar "Mulai percakapan"). Saat WS `open`, store kirim `INTRO_PROMPT` DIAM-DIAM (bukan bubble user — echo `user` diabaikan `parseSdkMessage`) yang nyuruh Claude paham konteks proyek lalu menyapa user & nanya mau usulin apa. Catatan: Agent SDK OTOMATIS nge-load `CLAUDE.md` ke context tiap sesi (fitur Claude Code), jadi Claude udah paham konteks tanpa harus manggil tool Read — INTRO_PROMPT cukup buat mengarahkan dia menyapa duluan. `busy:true` diset sejak start biar indikator "mengetik" muncul selama init.

**Render markdown di bubble assistant (2026-08-11):** balasan Claude berformat markdown → dirender jadi HTML rapi via `lib/markdown.ts` `renderMarkdown` (`marked` parse + `DOMPurify` sanitasi). CUMA bubble assistant (`{@html renderMarkdown(m.text)}` dalam wrapper `.cr-md`); bubble user tetap plain text (`{m.text}`) — input user jangan diinterpretasi. **Ini deps runtime PERTAMA di frontend** (`dependencies` tadinya kosong): `marked`, `dompurify` (+ `jsdom` devDep buat test — test file pakai docblock `// @vitest-environment jsdom`). Sanitasi WAJIB (marked v5+ teruskan raw HTML) — di-test strip `<script>`/`onerror`. `renderMarkdown` browser-safe (`typeof window` guard, sanitasi cuma di browser; prerender Node gak pernah render pesan chat). Link dapat `target=_blank rel=noopener` via hook DOMPurify. Style `.cr-md` (heading/list/code/pre/tabel) pakai `:global(...)` karena `{@html}`. Kalau ada tempat lain butuh render markdown, pakai `lib/markdown.ts` yang sama. Detail: `docs/decision-log/decision-log-chat-markdown-rendering-20260811.md`.

**Tampilan chat (2026-08-11):** bubble kiri/kanan (`.cr-row`/`.cr-bubble` — user rata kanan aksen `--win-blue` teks putih, assistant rata kiri `--content-alt`; tool/status jadi `.cr-note` samar di tengah). Input pakai `<textarea>` auto-grow (`.cr-textarea`, tinggi ngikutin isi sampai 160px lalu scroll): **Enter = kirim, Shift+Enter = baris baru** (`onKeydown`), tinggi di-reset abis kirim. Auto-scroll ke bawah tiap pesan/status berubah (`scrollToBottom` + `tick`). Ini interaksi modern di atas tema retro — sesuai aturan "retro itu visual doang".

**Lampiran gambar/screenshot di chat (2026-08-11).** Prompt WS bisa bawa gambar: `{type:'prompt', text, images:[{media_type, data}]}` (`data` = base64 MENTAH tanpa prefix `data:`). `claude-chat-service` (`sessionManager.sendPrompt`) menyusunnya jadi content block array Anthropic (`text` + `image` base64) kalau ada gambar, else tetap string (backward-compatible) — perlu **rebuild+restart service** tiap ubah sub-project ini (`npm run build` lalu restart proses host). Frontend: `ChangeRequestChat.svelte` punya tombol 📎 (file picker) DAN paste clipboard (`on:paste` di input — screenshot biasanya di-paste), preview thumbnail + hapus, render gambar di bubble user. Validasi tipe (png/jpeg/gif/webp) via `isSupportedImageType` (pure, tested) + guard 5MB/gambar. `ChatMsg` user variant dapat `images?: string[]` (data URL buat display); `buildTranscript` nandain `_[N gambar dilampirkan]_`. Diverifikasi end-to-end live: kirim PNG merah → Claude jawab "Merah". Backend proxy TIDAK berubah (WS tunnel raw, base64 gede aman).

**Sesi chat PERSIST lintas navigasi menu (2026-08-11).** State sesi (session id, WebSocket, messages, cost, step) hidup di store level modul `lib/stores/chatSessionStore.ts` — BUKAN di dalam komponen `ChangeRequestChat.svelte`. Alasan: kalau di komponen, `onDestroy` bakal nutup WS + `DELETE` sesi tiap user pindah menu → sesi mati padahal user belum mau nutup. Sekarang komponen cuma "view" yang subscribe; sesi baru ditutup lewat aksi EKSPLISIT: tombol "Akhiri sesi" (`closeSession()` → WS close + DELETE + reset ke idle) atau logout (`authStore.logout` panggil `resetAll()`). Visibility panel digerakkan `$chatSession.step !== 'idle'` (bukan flag lokal), jadi keluar/masuk `/change-requests` otomatis munculin panel + history. Titik hijau `.chat-live-dot` di tab "Change request" (topbar) nandain sesi hidup dari menu mana pun. Persist cuma lintas navigasi SPA — full reload reset store (WS mati, sesi service in-memory juga, konsisten). WS putus tak sengaja (service restart) TIDAK hapus history (user tetap bisa simpan transcript, cuma dikasih banner error). Logic murni yang ditest: `appendChatEvent`/`buildTranscript` di `lib/chatMessages.ts`. **Kalau nambah state yang harus hidup lintas navigasi (bukan cuma chat), pakai pola store level modul ini — JANGAN taruh di komponen dengan cleanup di `onDestroy`.**

**Gotcha Docker-dev + host build:** container `rndops-dev-frontend-1` (`node:20-alpine`) jalan sebagai ROOT, jadi begitu ada ROUTE BARU, vite dev di dalamnya nge-generate `.svelte-kit/types/.../<route>/$types.d.ts` sebagai root — `npm run build` di HOST (user `lugidev`) lalu gagal `EACCES` gak bisa overwrite file root itu. Bukan bug kode. Solusi tanpa sudo: build DI DALAM container (`docker exec rndops-dev-frontend-1 sh -c "cd /app && npm run build"`) — env yang sama yang punya file itu. (Alternatif: `make down-dev` dulu sebelum build host, tapi build-in-container lebih cepat buat verifikasi.)

**Gotcha/bug ketemu 2026-08-10 — Weekly Plan awalnya TIDAK difilter per user sama sekali** (ketauan pas user nanya "ini udah identik sama user id-nya kan??" — dicek ternyata BUKAN, semua user login lihat baris yang sama persis). "My Weekly Plan" itu laporan PRIBADI ke HR (bukan rollup tim, itu peran Dashboard) — sekarang `List`/`Push` di-JOIN dengan `dt.pic_user_id = <user login>`, jadi Big Task cuma muncul kalau requesting user PIC di minimal satu Daily Task-nya, dan `actual_pct` yang dihitung/di-push CUMA dari Daily Task dia (bukan agregat semua PIC di Big Task itu — Big Task multi-PIC akan muncul beda-beda di Weekly Plan tiap orang, itu WAJAR bukan bug). Lihat `docs/decision-log/decision-log-weekly-plan-scope-per-user-20260810.md`. **Kalau nambah endpoint baru yang namanya/konteksnya personal ("My ..."), JANGAN asumsikan filter per-user otomatis ada cuma karena butuh auth token — auth token cuma identifikasi SIAPA yang request, query-nya sendiri harus eksplisit di-filter `WHERE`/`JOIN ... = userID`.**

**Access Level & HR Mapping (2026-08-10):** `users.access_level` (`super_user`/`regular_user`, default `regular_user`) — SENGAJA kolom tunggal (exception dari aturan role many-to-many, lihat "Jangan lakukan" di bawah), masuk JWT claims (`auth.AccessLevelFromContext(ctx)`/`auth.IsSuperUser(ctx)`) di samping `roles`. `referensi_tim` (id, name — dropdown "Tim/Org" ganti free-text, `users.org_team` TETAP text apa adanya, cuma divalidasi terhadap tabel ini) dan `referensi_user_hr` (hr_user_id, email, nip, nama_lengkap — data MILIK sistem HR asli, di-seed migration `0013` PERSIS dari export yang diberikan, TIDAK ADA CRUD UI) adalah dua tabel referensi baru. `users.hr_user_id` (nullable, unique FK ke `referensi_user_hr`) di-mapping lewat Manajemen User — begitu ke-mapping, `weeklyplan.pushToMyAgenda` pakai `hr_user_id` ASLI (bukan placeholder CRC32 lagi, lihat `resolveHRUserID`).

**Super User bisa lihat/push Weekly Plan siapapun**: `GET /weekly-plan`/`POST /weekly-plan/push` terima `as_user_id` opsional — CUMA dihormati kalau `access_level=super_user`, else 403 (fail-loud, bukan diabaikan diam-diam — lihat `resolveTargetUserID`, logic murni yang dites terpisah dari context/JWT). `weekly_push_log.pushed_by` SELALU actor (siapa yang benar-benar klik), BUKAN `as_user_id` target — audit trail. `GET /weekly-plan/team-status` (super_user only) kasih ringkasan semua user (termasuk yang 0 Big Task minggu ini) — `total_big_tasks`/`pushed_big_tasks`/`all_pushed`, dipakai buat "cek tim udah push atau belum" tanpa drill-down satu-satu. Frontend `routes/weekly-plan/+page.svelte`: dropdown "Lihat sebagai" + tabel "Status push tim" CUMA render kalau `$auth.user.access_level === 'super_user'`.

**Manajemen User (SettingsModal) sekarang bisa EDIT user existing** (`PATCH /users/{id}`, admin-only) — sebelumnya cuma bisa `POST /users` bikin baru, gak ada cara ubah `org_team`/`access_level`/`hr_user_id`/role user lama. Tabel di tab "Manajemen user" sekarang 7 kolom (Nama/Email/Role/Tim/Access/Mapping HR/Aksi) — modal-nya dapat class `.modal-box-user-mgmt` (820px, CUMA aktif kalau tab ini) + `.modal-body-scroll-x` di pembungkus tabel, biar gak overflow keluar modal kayak `.modal-box-wide` biasa (480px, terlalu sempit buat 7 kolom).

**Import / Export data (super_user only, 2026-08-12):** `backend/internal/dataport/` — `GET /admin/export` (stream file XLSX) dan `POST /admin/import` (multipart/form-data, field `file`, return JSON stats). Format ekspor: workbook 4 sheet — **Boards** (Name | Description), **BigTasks** (Board | Name | StartDate | Deadline | OnHold | Members), **DailyTasks** (Board | BigTask | Title | PICEmail | StartDate | EndDate), **DayEntries** (Board | BigTask | DailyTask | EntryDate | PlannedText | ProgressPct | BlockerText). Import resolusi user via email; jika PIC Daily Task tidak ditemukan → task di-skip + warning; jika anggota Big Task tidak ditemukan → diabaikan + warning (task tetap dibuat). OnHold ditulis "Ya"/"Tidak", Members comma-separated emails. Frontend: `routes/import-export/+page.svelte` (super_user only, aksesnya dari dropdown user di topbar). **Import endpoint menerima multipart** — frontend pakai `api.upload('/admin/import', formData)`, bukan `api.post` biasa (karena binary file, bukan JSON). Library: `github.com/xuri/excelize/v2 v2.8.1`.

**Board Archive (super_user only, 2026-08-12):** `boards.archived_at`/`archived_by` (migration `0020`, existence-pattern seperti sign-off) — `board.List` (`GET /boards`, dipakai BARENG Dashboard & tab Boards) filter `WHERE archived_at IS NULL`, jadi board archived hilang dari keduanya sekaligus lewat satu titik perubahan. **Weekly Plan & Review Queue SENGAJA TIDAK difilter** (query di situ JOIN langsung ke `boards`/`daily_tasks`, gak lewat `board.List`) — board archived TETAP muncul apa adanya di sana, karena laporan personal ke HR & antrean review adalah level TASK bukan level project, gak boleh "kehilangan" riwayat cuma karena project-nya diarsipkan. `board.ListArchived`/`Archive`/`Unarchive` (route baru, `GET/PATCH /boards/archive`, `.../archive`, `.../unarchive`) cek `auth.IsSuperUser(ctx)` in-handler — pola sama seperti `weeklyplan.TeamStatus`, BUKAN `RequireRole` middleware (`access_level` beda axis dari `roles`). Frontend: nav "🗄 Board Archive" di dropdown user panel (`+layout.svelte`, di antara "My Profile" dan "Settings", `<a>` beneran ke route `/boards/archive` — bukan modal, karena isinya listing+aksi) gated `isSuperUser`; tombol "🗄 Archive board" di `boards/+page.svelte` (pojok kanan `.board-pills-row`, cuma render kalau super_user & ada board terpilih) buka inline-confirm (bukan `window.confirm()`) sebelum `PATCH .../archive`. Detail & alasan lengkap: `docs/decision-log/decision-log-board-archive-20260812.md`.

**Gotcha ketemu 2026-08-10 — sessionStorage cache di `authStore.init()` bikin `access_level` (dan field User apapun yang ditambah ke depannya) kelihatan HILANG sampai user logout manual, walau DB & JWT-nya sudah benar.** `init()` DULU cek cache `sessionStorage` ("np_user") dulu dan CUMA fetch `GET /users/me` kalau cache-nya kosong — kalau user sudah pernah login SEBELUM field baru (`access_level`) ditambahkan ke bentuk `User`, cache lama di browser-nya gak punya field itu, dan reload/buka-tutup-tab TIDAK memicu re-fetch (sessionStorage persist antar reload, cuma clear kalau tab/window ditutup) — jadi fitur `super_user` kelihatan gak jalan ("gue ga bisa cek user lain") padahal backend-nya sudah benar, root cause-nya cuma cache browser basi. Diverifikasi cache ini TIDAK render apapun ke UI selama status `loading` (`+layout.svelte` cuma nunjukin teks "Memuat sesi...", gak baca cached user sama sekali) — jadi caching ini nggak pernah benar-benar mencegah flicker apapun, cuma murni risiko staleness tanpa manfaat. **Fix: `init()` SEKARANG SELALU fetch fresh `GET /users/me`, gak pernah skip-fetch pakai cache lagi** (`loadCachedUser` dihapus). Kalau nambah field baru ke `UserSummary`/`User` ke depannya, TIDAK perlu khawatir soal cache basi lagi — tapi kalau nanti ada alasan kuat buat cache-first lagi (misal buat offline-first), inget kejadian ini dulu.

**Gotcha ketemu 2026-08-10 (regresi lama, gak sengaja ke-expose pas nambah fitur ini): default tab SettingsModal salah.** `let activeTab = canManage ? 'users' : 'theme'` dieksekusi SEBELUM reactive statement `$: canManage = ...` pernah jalan sekali pun (plain `let` di atas jalan duluan, baru reactive block jalan) — jadi `canManage` masih `undefined` di titik itu, `activeTab` SELALU jatuh ke `'theme'` walau user-nya admin/spv. Fix: default polos `let activeTab = 'users'` (TANPA cek `canManage` di titik init) — aman karena template sudah double-check `canManage` sendiri (`{#if activeTab === 'users' && canManage}`). Pola ini ("`let x = someReactiveVar ? a : b`" di top-level) JANGAN dipakai lagi — reactive var yang direferensikan belum ke-compute di titik itu.

**Frontend Weekly Plan** (`routes/weekly-plan/+page.svelte`): navigasi minggu pakai `getWeekStart`/`shiftWeek`/`weekEnd` di `lib/dateRange.ts` (fungsi murni, ada test-nya — "Senin sebagai awal minggu" per `06-db-design.md` §3.11, edge case Minggu/Sunday `getUTCDay()===0` wajib mundur ke Senin minggu ITU, bukan minggu depan). Default `weekStart` dihitung dari `new Date()` sekali di top-level `let` (bukan reactive), navigasi prev/next cuma reassign variabel biasa.

**Review Queue (Fase 7; scope DIUBAH LAGI 2026-08-11):** `backend/internal/reviewqueue/` — `GET /review-queue` dan `POST /review-queue/{item_type}/{item_id}/mark-reviewed`, cuma `item_type = "daily_task"`. **Isi queue SEKARANG = TASK REVIEW** (daily task hasil clone-review, `review_of_daily_task_id != NULL`) yang **PIC-nya = requesting user** & belum ada di `item_reviews`. Tidak lagi pakai `big_task_reviewers`/fallback spv (DIGANTIKAN — reviewer = orang yang di-assign lewat clone-review, yaitu PIC task review). Response `Item` punya kolom `source_daily_task_title` (daily task asal yang direview) buat konteks — ditampilkan di `routes/review-queue/+page.svelte` ("Mereview daily task: X"). `MarkReviewed` otorisasi EKSPLISIT: requesting user harus PIC dari task review itu (`pic_user_id = me AND review_of_daily_task_id IS NOT NULL`). Tabel `item_reviews` (migration `0008`) tetap sama: keberadaan baris = sudah ditinjau, idempoten `ON CONFLICT (item_type, item_id)`. Lihat `decision-log-bigtask-members-refactor-20260811.md`.

**Anggota Big Task (2026-08-11, MENGGANTIKAN konsep "Reviewer per Big Task"):** tabel `big_task_members (big_task_id, user_id)` (di-rename dari `big_task_reviewers`, migration `0015`) = **siapa saja yang terlibat/menangani** Big Task (tim, komposisi role bebas) — BUKAN khusus reviewer. `bigtask.BigTask.MemberUserIDs []string` (json `member_user_ids`) di response. **WAJIB min. 2 anggota** saat create (`dedupeMembers` + tolak 400 kalau <2). Anggota **editable** lewat `PUT /big-tasks/{id}/members` body `{member_user_ids}` (replace-set, tetap min-2) — dipakai `SetMembers`, sekaligus jalur merapikan data lama (big task lama `member_user_ids: []` di-grandfather, gak diutak-atik). **PIC Daily Task WAJIB anggota** (validasi di `dailytask.Create` + `CloneReview`, 400 kalau bukan; daily task lama di-grandfather). Reviewer clone-review juga wajib anggota. Frontend: `BigTaskList.svelte` form create punya checkbox "Anggota" (min 2, Simpan disable kalau <2); detail Big Task ada **summary tim** (`.member-summary`, avatars+nama+role di atas daftar daily task) + tombol "Kelola anggota" (editor `.member-editor` → PUT members); `activeMembers` diteruskan sebagai prop `members` ke `DailyTaskPanel` (buat scope PIC picker + reviewer picker). Lihat `docs/decision-log/decision-log-bigtask-members-refactor-20260811.md`. **`decision-log-bigtask-reviewer-assignment-20260810.md` DIGANTIKAN** oleh keputusan ini.

**Frontend Review Queue pakai shared store** `lib/stores/reviewQueueStore.ts` (`reviewQueue`, `loadReviewQueue()`, `markItemReviewed()`) — dipakai BARENG oleh `+layout.svelte` (badge notifikasi FR-NTF-01 di tab "Review queue" dan dropdown bell) dan `routes/review-queue/+page.svelte`. **Sekarang di-load buat SEMUA user yang login** (`$auth.status === 'authenticated'`), bukan cuma yang ber-role spv lagi — backend yang nentuin isi queue-nya kosong/tidak per user. Store dipakai (bukan local state di masing-masing) supaya mark-reviewed dari halaman langsung kepantul ke badge di topbar — pola staleness yang sama seperti `BigTaskList`↔`DailyTaskPanel` di Fase 1, tapi di sini pakai store karena dua route sibling (bukan parent/child) tidak bisa saling dengar event Svelte langsung.

**Gotcha Svelte (keyed `{#each}` — JANGAN key pakai field yang bukan identitas unik, mis. `.name`):** `GroupedBarChart.svelte` sempat key pakai `d.name` (nama Big Task) — begitu ada DUA Big Task beda (id beda) yang kebetulan namanya sama (skenario nyata, bukan cuma edge case: dua board bisa sama-sama punya Big Task "Tahap Analisis"), Svelte CRASH runtime dengan "Cannot have duplicate keys in a keyed each". Ketemu & diverifikasi langsung waktu testing Fase 7 (dua board test dengan nama sama). Fix: key pakai index array (`(i)`) untuk chart yang datanya cuma buat render visual tanpa butuh stable identity lintas re-render — `DonutChart.svelte` ikut diubah ke pola yang sama untuk jaga-jaga meskipun datanya saat ini selalu kategori tetap (aman). Kalau nambah `{#each}` baru yang keyed, SELALU pastikan key-nya benar-benar unik (id dari backend), jangan pernah pakai display text (nama, judul, label) sebagai key.

## Jangan lakukan

- Jangan simpan `verdict`, `expected_pct`, `actual_pct`, `completion_rate`, atau `is_weekend` sebagai kolom tabel — wajib tetap computed-at-read (lihat penjelasan di atas).
- Jangan tambahkan kolom `role` tunggal di `users` — role itu many-to-many via `user_roles`. **Exception sadar (2026-08-10)**: `users.access_level` (`super_user`/`regular_user`) MEMANG kolom tunggal — ini keputusan eksplisit user, konsepnya beda dari `roles` (saling eksklusif, bukan bisa dirangkap). Lihat bagian "Access Level & HR Mapping" di bawah. Jangan tiru pola ini buat konsep baru tanpa alasan setara (saling eksklusif) — kalau ragu, tanya user dulu kayak kasus ini.
- Jangan ganti auth ke cookie/session — ini API stateless Bearer-JWT by design.
- Jangan tambahkan layer abstraksi service/repository di handler backend — modul yang sudah ada query `pgx` langsung, modul baru harus ikut pola itu, jangan bikin pola baru.
- Jangan hapus `preprocess: vitePreprocess()` di `frontend/svelte.config.js` — tanpa itu `npm run build` gagal total begitu ada sintaks TypeScript (`type X = ...`, `as Y`) di dalam `<script lang="ts">` manapun (sempat kejadian, baru ketahuan Fase 0 karena belum pernah ada yang coba build).
- Jangan taruh `<slot/>` root layout di cabang `{#if}` terpisah per state auth — lihat gotcha di bagian Auth di atas.
- Jangan bikin halaman baru dengan styling ad-hoc (warna/spacing sendiri) — pakai token & class di `app.css` (lihat bagian Design System). Semua halaman yang sudah ada sudah di-re-skin mengikuti `docs/rnd-ops-mockup_3.jsx`, jangan mundur ke gaya polos lagi.
- Jangan tambah library chart (Recharts/Chart.js/dst) — chart yang dibutuhkan (donut, grouped bar) sudah di-cover `DonutChart.svelte`/`GroupedBarChart.svelte` custom SVG. Lihat decision log kalau kebutuhannya berubah jauh lebih kompleks.
- Jangan bikin ulang halaman/route buat "My Profile" atau "Settings" — itu sudah jadi modal (`ProfileModal.svelte`/`SettingsModal.svelte`) dipanggil dari dropdown user di topbar, bukan route SvelteKit.
